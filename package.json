{
  "name": "saucedemo",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
  "test": "playwright test",
  "clean:reports": "rm -rf allure-results allure-report || rmdir /s /q allure-results allure-report",
  "report": "allure generate ./allure-results --clean && allure open ./allure-report",
  "test:allure": "npm run clean:reports && playwright test && npm run report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.53.0",
    "@types/node": "^24.0.3",
    "allure-playwright": "^3.2.2",
    "playwright": "^1.53.0"
  }
}
